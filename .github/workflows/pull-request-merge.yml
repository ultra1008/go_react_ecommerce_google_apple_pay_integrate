name: Automate Pull Request and merge

on:
  push:
    branches:
      - master

jobs:
  create_pull_request:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v3
      with:
        token: "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"
        branch: 'automated-pr-branch'
        title: 'Automated PR'
        body: 'This is an auto-generated PR for recent changes.'
        labels: 'auto-pr, automerge'

  auto_merge:
    runs-on: ubuntu-latest
    needs: create_pull_request
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Merge pull request
      uses: pascalgn/automerge-action@v0.11.0
      env:
        GITHUB_TOKEN: "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"